aiohttp>=3.9,<4
aiosqlite>=0.20,<1
fastapi>=0.115,<1
grpcio>=1.70,<2
# Codegen only (used to regenerate proto_gen/*). The protobuf runtime below
# must be >= the gencode version this emits; the committed stubs are gencode
# 6.31.1, so the protobuf runtime is pinned to 6.31+.
grpcio-tools>=1.70,<2
jinja2>=3.1,<4
protobuf>=6.31,<7
python-multipart>=0.0.9,<1
sqlalchemy[asyncio]>=2.0,<3
uvicorn[standard]>=0.34,<1
