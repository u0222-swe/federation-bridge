# Development / test dependencies (install alongside requirements.txt):
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest tests/ -v
pytest>=8,<9
pytest-asyncio>=0.23,<1
httpx>=0.27,<1   # for fastapi.testclient.TestClient (endpoint tests)
